> 以下为发布前虚构情境的模型生成示例，尚未由学生执行，不代表实际工程结果或录用效果。

> 编辑补注：示例中的输入幅度减半与稳态窗口比较，应先确认已有工程是对应的瞬态分析；若只有AC曲线，应按AC激励与频率响应定义设计复测。原始评测答复和评分保持不变。

这段经历可以诚实地写成**“开源运放教程仿真复现”**。现有信息不能支持“独立设计量产芯片”或“LVS 全通过”，这两项不应写进简历。下周按你实际的8小时净时间安排，任务6.4小时、缓冲1.6小时，20小时的愿望先缩成一个可完成的证据补全任务。

如果你希望投模拟 IC 设计，相关能力包括五项：偏置与小信号分析用于解释电路；测试平台与参数设置用于获得可信结果；规格和边界验证用于判断是否达标；故障定位用于说明个人贡献；版图、寄生与物理验证用于把原理图推进到实现。当前项目能够支持前几项的入门练习。目标岗位尚未确认，这里只是候选，不假定你的学制、导师方向或已有授权工具。

可立即采用的保守简历条目是：

> **开源运放教程仿真复现｜个人学习项目**  
> 依据开源教程完成运放示例的仿真复现，获得教程所设典型条件下的输出波形；本次实践范围为示例运行与波形观察。

正式使用前补项目或教程名称、来源链接和真实起止日期。若现有仿真只是行为模型或厂商宏模型，标题再注明“行为/宏模型仿真”；只有核对到晶体管网表及模型，才可以具体写相应层次。现在没有可追溯的增益、带宽、功耗结果，就暂不加数字。也不添加“分析参数影响”“自动化验证”“独立搭建测试平台”等你尚未说明完成的动作。

**把已有成果和待验证项分开。** 你报告本周投入4小时并照教程跑通，这是已有进展；我尚未审阅输入、日志与波形，暂记“教程复现，证据待核查”。还不能确认你是否能够独立重跑或解释结果，所以不直接评为 L3。若能按文档重跑并解释输入输出，可达到局部 L2；L3 需要独立完成明确任务、定位故障并提交复现证据。

| 项目结论 | 目前能怎样记录 |
| --- | --- |
| 示例运行 | 已完成，自述；输入和日志待审阅 |
| 典型条件波形 | 已有；电压、温度、负载、激励、模型版本待补 |
| 指标达标 | 待验证；波形存在不能自动证明达标 |
| 多条件、统计或寄生后仿真 | 未提供，不计完成 |
| 版图、DRC、LVS | 未完成；没有版图及真实检查报告 |
| 流片、实测、量产 | 未完成，不列为项目经历 |

LVS 是版图与原理图/网表的一致性检查，需要真实比较对象、规则、检查器与报告。仿真运行成功没有完成这种比较。本次只能表达“在所用模型和典型条件下复现波形”，不能外推到工艺签核或生产表现。

**下周唯一主要成果：一份能够重跑、能解释范围的复现包。** 沿用现有工具及原教程，不另开新版图或第二个运放项目。时长估计允许根据第一项的实耗调整：

| 任务 | 时间 | 输入、动作、产物与验收 | 卡住时如何缩减 |
| --- | --- | --- | --- |
| A：恢复实验条件 | 2h | 输入现有教程、网表/工程及波形。记录工具版本、模型来源、供电、温度、负载、输入和分析类型；保存基线副本、运行步骤与原始日志。验收为每个数值可指向设置，未知处标未知。 | 找不到模型或工程时先恢复来源与输入；只整理已有截图的轴、单位、节点，不据图猜条件。 |
| B：一次单变量实验 | 2.8h | 先用1h从副本重跑基线；成功后，在既有模型适用范围内，只将输入幅度减半，其他设置固定。先写预测，再运行，保存前后数据，选同一稳态窗口比较幅值比及是否削顶。验收为能解释差别；明确这是该激励下的幅值比，不冒称开环增益。 | 基线不能复现就把剩余时间用于定位路径、模型、分析设置，取消变参。幅值变化难解释时检查直流偏置、量测窗口和饱和，不连续乱调多个参数。 |
| C：解释与复盘 | 1.6h | 输入 A/B 产物。写一页结果和限制、本人动作、失败记录；用90秒说明项目，再核对简历每个动词的证据。验收为另一人能按步骤找到输入和结果，未运行事项清楚可见。 | 文件不齐先做证据索引，不美化展示图；没有出现故障就如实写“本周未形成故障案例”，不编调试故事。 |

合计 **2+2.8+1.6=6.4小时**，再留1.6小时处理临时事务或必要排错。缓冲用完即停，余项迁移到下一周。你这周只有4小时，但没有上一版预算，不能据此认定“拖欠进度”；若下周实际仍只能投入4小时，保留 A 的2小时、C 的1.2小时及0.8小时缓冲，取消 B。

学习材料仅选原教程对应的输入设置和测量章节，以及你实际仿真器版本的官方说明。工具名称未提供，暂不让你换软件，也不编具体菜单或命令。项目的后续层级是：先完成最小复现，再做有自定规格和一个失败案例的核心验证，最后在资源具备时再选版图或更多条件；每次只升一级。

技能账本下周更新三行即可：仿真执行——当前教程复现自述，目标 L2，依赖工程完整，对应 A/B，以干净目录重跑验收；电路解释——待测，目标 L1，对应 B/C，以预测与结果解释验收；工程表达——待测，目标 L2，对应 C，以简历证据逐条对应验收。版图与 LVS 保留“未开展”，不安排在本周冲刺清单。

面试先练三问：哪些是教程给的，哪些是你做的？波形对应什么条件？仅典型条件能够与不能够证明什么？下周新增动作真正完成后，才可以加一句“整理复现条件并比较输入幅度变化下的响应”，结果数字仍从原始数据填。

**现在先做60分钟**：15分钟复制基线并保留原文件，25分钟填写条件表，20分钟整理输入与输出索引，计入任务 A。下周结束按实耗、文件、验收与阻塞复盘。需要补的信息最多两组：教程来源/工具版本/可公开的工程或日志；目标岗位/关键截止日期。没有答案也能从现有文件整理开始，缺失字段保持空白。

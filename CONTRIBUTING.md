# 贡献指南

本项目以可执行学习任务和可验证能力为核心。补充岗位时，请同时提交：职责与适配条件、P0/P1/P2能力、先修依赖、实践步骤、可检查产物、失败定位、工具资源边界、官方来源与获取日期。

修改安装方式时提供客户端名称、版本、操作系统、实际导入步骤和非敏感错误信息。只有真实完成导入和对话才能标记端到端通过。不要用平台宣传页替代客户端实测。

修改来源时优先企业招聘页、官方工具文档、大学课程和标准组织资料。招聘页面记录应届/实习/资深类别，不以单个JD推断行业通用标准。教学项目数值必须标为自定规格。

提交前运行 `python scripts/validate_release.py` 与 `python scripts/build_release.py`。涉及行为变化时用 `evals/evals.json` 的情境回归，并人工检查是否先给技能地图、时间预算是否闭合、是否有验收、是否出现虚构经历。

不接受破解软件、泄露PDK、未经授权的厂内数据、个人简历敏感信息、虚假招聘或保证就业的宣传。外部材料按原授权使用，本仓库原创内容按MIT许可贡献。

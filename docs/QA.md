# v1.0.0 验证范围

日期：2026-09-22。以下分别记录文件、对话输出与客户端验证，不能互相替代。

## 已完成

- 核心技能与资料结构检查：105行入口、13条岗位路线、39个分级项目、29张官方资源卡、9份学生模板。
- 本地相对链接、引用文件、UTF-8替换字符检查通过；具体由 `scripts/validate_release.py` 执行。
- 构建过程核查两平台包的核心正文一致、所有参考文件齐全、ZIP可读且无CRC错误；SHA256清单随发布包提供。
- 三个情境各生成带Skill和普通助手基线答复，共6份；独立评审逐条检查15项要求，结果与原始答复见 `evals/`、`examples/` 和 `docs/eval-analysis.md`。这些是模型答复检查，学生项目未实际执行。
- Word手册包含28页、22张表。已用本机Word导出检查版并逐页检查全部28页，无截断、溢出或字体缺失。分段记录见本目录的三个visual-qa文件。

## 未完成或不能由上述检查证明

- 未在真实Codex客户端完成自动发现安装与新对话端到端验收；测试代理按文件读取了Skill。
- 未在真实WorkBuddy客户端导入ZIP并完成对话验收；ZIP最外层结构是本项目选择，官方页面未明确这一约束。
- 未安装或实跑所有EDA/TCAD/测试工具，未执行39个学生项目；项目规格和时间是教学建议。
- 未证明就业率、薪资、企业录用门槛或当前地区岗位供给；资源页可访问不等于本机环境可运行。
- 未收集可比的执行耗时、token用量或跨模型重复实验数据，评测不提供这些数值。

评审后在复盘参考中补充了“先确认DC/AC/瞬态等分析类型再设计复测”的说明，并对公开的第3个示例添加编辑补注。原始评测输出及评分未修改，也没有将此补充声称为额外重测结果。

## 工具执行记录

标准 `render_docx.py` 因本机缺少LibreOffice可执行文件未能渲染，改用本机Word只读导出PDF，再用Poppler生成页面图像。最终28页已实际检查。检查用PDF与PNG保留在本地 `.qa/`，不作为发布附件。

skill-creator的 `quick_validate.py` 因其PyYAML依赖缺失未运行成功。本项目使用自己的结构与引用校验脚本，以及实际构建后的ZIP内容核对；不把它标为官方验证器通过。

## 发布者的五分钟客户端检查

在自己常用客户端中按INSTALL安装，确认技能可发现、参考文件可读取；输入研一示例，检查技能地图、时间预算和验收；补充“没有商业EDA”与“本周只有6小时”，检查是否正确降级和重排。记录客户端版本和结果后再更新本文件。未完成的项保持未测，不用宣传文案替代证据。
